# Server
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rag_pipeline

# =============================================================================
# Embedding Provider Configuration
# =============================================================================
# Choose ONE of the following options:
#
# Option 1: OpenAI (default if OPENAI_API_KEY is set)
# - Best for: General use, well-documented, widely supported
# - Models: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
#
# Option 2: Voyage AI (recommended for Claude/Anthropic users)
# - Best for: Users with Anthropic API access, specialized domains (code, legal, finance)
# - Models: voyage-3, voyage-3-lite, voyage-code-3, voyage-finance-2, voyage-law-2
# - Can use either VOYAGE_API_KEY or ANTHROPIC_API_KEY
#
# Option 3: Mock (for development/testing without API keys)
# - Set EMBEDDING_PROVIDER=mock to use deterministic mock embeddings
# =============================================================================

# Embedding provider (auto-detected from API keys if not set)
# Options: openai, voyage, mock
# EMBEDDING_PROVIDER=openai

# OpenAI API Key (for OpenAI embeddings)
OPENAI_API_KEY=sk-your-key-here

# Voyage AI API Key (for Voyage embeddings)
# Get your key at: https://dash.voyageai.com/
# VOYAGE_API_KEY=pa-your-key-here

# Anthropic API Key (can also be used for Voyage embeddings)
# If you have a Claude Console API key, you can use it for Voyage embeddings
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# AWS (optional, for S3 source type)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# CORS
CORS_ORIGIN=http://localhost:5173
